#!/bin/zsh

eval "$(/opt/homebrew/bin/brew shellenv)"

if [[ -e "$HOME/.ssh/ca-bundle.crt" ]]
then
  export NODE_EXTRA_CA_CERTS=$HOME/.ssh/ca-bundle.crt
fi

export PATH="$HOME/.config/bin:$HOME/.local/bin:$PATH"

source "$HOME/.config/zsh/alias"

